---
import { Icon } from "astro-icon/components";

interface Props {
  name: string;
  title: string;
}

const { name, title } = Astro.props;
---

<Icon {name} {title} aria-hidden="true" class="size-6" />
