---
import { Icon } from "astro-icon/components";

interface Props {
  name: string;
  title: string;
}

const { name, title } = Astro.props;
---

<Icon name={name} title={title} aria-hidden="true" class="h-6 w-6" />
