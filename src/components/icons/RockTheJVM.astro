---
import site from "@/data/site.json";
import { Icon } from "astro-icon/components";

const { suffix = "", ...rest } = Astro.props;
---

<Icon
  name=`companies/rock-the-jvm${suffix}`
  title={site.name}
  aria-hidden="true"
  {...rest}
/>
