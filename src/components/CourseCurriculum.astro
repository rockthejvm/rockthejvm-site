---
import CourseCurriculumHelper from "./CourseCurriculumHelper";

interface Props {
  teachableId: string;
  courseSlug: string;
}

const { teachableId, courseSlug } = Astro.props;
---

<CourseCurriculumHelper teachableId={teachableId} {courseSlug} client:load />
