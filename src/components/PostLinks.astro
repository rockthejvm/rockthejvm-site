---
import { getCollection } from "astro:content";
import PostLink from "../components/PostLink.astro";

const posts = await getCollection("posts");
---

<ul>
  {posts.map((post) => <PostLink slug={post.slug} title={post.data.title} />)}
</ul>
