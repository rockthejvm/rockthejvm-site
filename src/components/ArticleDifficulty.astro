---
interface Props {
  difficultyId: string;
}
const { difficultyId } = Astro.props;
---

<a
  href={`/difficulties/${difficultyId}`}
  class="relative z-10 rounded-full bg-tag px-3 py-1.5 text-center font-medium hover:bg-gray-100"
>
  <p class="text-indigo-800">{difficultyId}</p>
</a>
