---
import Prose from "@/components/Prose.astro";

interface Props {
  class?: string;
  color?: string;
}

const { class: additionalClasses } = Astro.props;
---

<Prose class="mx-auto">
  <h2 class={`bg-transparent py-8 sm:py-10 text-center ${additionalClasses}`}>
    <slot />
  </h2>
</Prose>
