---
import type { CollectionEntry } from "astro:content";
import Base from "./Base.astro";

interface Props {
  frontmatter: CollectionEntry<"informationals">["data"];
}

const { frontmatter } = Astro.props;

const { description, title } = frontmatter;
---

<Base {description} {title} type="article">
  <!-- <Hero
    image={{
      alt: site.logo.alt,
      src: site.logo.src,
    }}
    {title}
  /> -->
  <slot />
</Base>
