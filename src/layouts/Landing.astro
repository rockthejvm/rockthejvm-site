---
import Hero from "../components/Hero.astro";
import Base from "./Base.astro";

interface Props {
  image?: { src: string; alt: string };
  metaDescription: string;
  metaTitle: string;
  OGDescription: string;
  OGImage: string;
  OGImageAlt: string;
  OGTitle: string;
  OGType: string;
  OGURL: string;
  subtitle: string;
  subtitle2?: string;
  title: string;
}

const {
  image,
  metaDescription,
  metaTitle,
  OGDescription,
  OGImage,
  OGImageAlt,
  OGTitle,
  OGType,
  OGURL,
  subtitle,
  subtitle2,
  title,
} = Astro.props;
---

<Base
  {metaDescription}
  {metaTitle}
  {OGDescription}
  {OGImage}
  {OGImageAlt}
  {OGTitle}
  {OGType}
  {OGURL}
>
  <Hero {image} {subtitle} {subtitle2} {title} />
</Base>
<!-- <style>
  Hero {
    background-color: var(--color-primary);
  }
</style> -->
